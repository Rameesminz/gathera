# Local development (not loaded during production builds)
NEXT_PUBLIC_API_URL=http://localhost:8788/api/v1
NEXT_PUBLIC_WS_URL=ws://localhost:8788/api/v1

# Production builds use .env.production (see that file)
